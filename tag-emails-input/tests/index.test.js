describe("component Tagify", () => {
  describe("EmailsEditor", () => {
    it("is possible to use multiple instances", () => {
      // expect(wrapper.find('.gl-badge').exists()).toBe(true)
    });
    it("should create an email tag upon key [Enter] [Comma] or [Blur]", () => {
      // expect(wrapper.find('.gl-badge').exists()).toBe(true)
    });
    it("should create a word tag upon key [Enter] [Comma] or [Blur]", () => {
      // expect(wrapper.find('.gl-badge').exists()).toBe(true)
    });

    it("can add emails manually", () => {
      // expect(wrapper.find('.gl-badge').exists()).toBe(true)
    });

    it("can count emails manually", () => {
      // expect(wrapper.find('.gl-badge').exists()).toBe(true)
    });
  });
});
